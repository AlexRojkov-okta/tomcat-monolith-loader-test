/**
 * 1. should support a maven module / dependencies graph
 * 2. should be able to start at any point of the graph and build up-stream and down-stream dependencies
 * 3. should group or filter dependencies by scope
 * 4. should filter dependencies by profile
 * 5. should not discover siblings
 * 6. should be fast
 */